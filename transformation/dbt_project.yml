name: cfm_flights
version: "1.0.0"

profile: cfm_flights

model-paths: ["models"]
macro-paths: ["macros"]
clean-targets:
  - "target"
  - "dbt_packages"

models:
  cfm_flights:
    marts:
      +materialized: table